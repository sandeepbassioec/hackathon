# TransitOps Frontend

React + Vite SPA that consumes the backend REST API (`/api/...`).

Scaffold with:
```
npm create vite@latest . -- --template react
```

Pages to build: Login, Dashboard (KPIs + filters), Vehicles, Drivers, Trips,
Maintenance, Fuel & Expense, Reports.
